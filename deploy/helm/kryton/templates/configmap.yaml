apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kryton.fullname" . }}-images
  labels:
    {{- include "kryton.labels" . | nindent 4 }}
data:
  images.json: |
    {{ dict "images" .Values.images | toPrettyJson | nindent 4 }}
