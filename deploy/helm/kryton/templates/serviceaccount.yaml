{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kryton.serviceAccountName" . }}
  labels:
    {{- include "kryton.labels" . | nindent 4 }}
{{- end }}
