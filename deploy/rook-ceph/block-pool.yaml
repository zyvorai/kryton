apiVersion: ceph.rook.io/v1
kind: CephBlockPool
metadata:
  name: replicapool
  namespace: rook-ceph
spec:
  # --lab script patches failureDomain=osd and size=1
  failureDomain: host
  replicated:
    size: 3
    requireSafeReplicaSize: true
