## What & why

<!-- What does this change do, and what problem does it solve? -->

## Checklist

- [ ] `make check` passes locally (fmt, test, vet, build)
- [ ] Tests added/updated for lifecycle and error paths (see `CONTRIBUTING.md`)
- [ ] Provider-specific behavior stays behind `internal/provider.Provider`
- [ ] New source files have the Apache-2.0 header (`./scripts/add-license-headers.sh`)
- [ ] Docs updated (`README.md`, `docs/`) if this adds API fields, providers, or configuration
- [ ] `CHANGELOG.md` has a line under `## [Unreleased]` for any user-visible change
